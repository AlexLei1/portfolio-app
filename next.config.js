/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
	poweredByHeader: false,
	optimizeFonts: false,
	experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
